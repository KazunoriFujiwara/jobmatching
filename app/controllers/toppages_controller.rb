class ToppagesController < ApplicationController
  def index
    #if clogged_in?
      #@job = current_company.jobs.build
      #@jobs = current_company.feed_jobs.order(id: :desc).page(params[:page])
    #end
    #redirect_to company_path(current_company) unless company_clogged_in?
  end
end
