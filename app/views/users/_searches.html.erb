<%= form_tag(searches_user_path,:method => 'get') do %>
  <%= text_field_tag :search %>
  <%= submit_tag 'Search', :name => nil %>
<% end %>

  検索ワード：　<%= params[:search] %>

<ul><% @jobs.each do |job| %>
  <li>
    <%= link_to job.content,job %>
    <%= job.created_at.strftime("%Y-%m-%d %H:%M") %>
  </li>
  <% end %>
</ul>